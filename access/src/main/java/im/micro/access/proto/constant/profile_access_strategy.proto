syntax = "proto3";

package im.micro.proto;

option java_package = "im.micro.access.constant";
option java_multiple_files = true;
// 个人简介常量
enum ProfileAccessStrategy {
    ALL = 0; // 所有人都可以访问
    ALL_EXCEPT_BLACKLISTED_USERS = 1; // 除了黑名单的所有用户都可以访问
    FRIENDS = 2; // 只有好友才能访问
}
